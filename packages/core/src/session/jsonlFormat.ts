/** Versioned header written at the start of every new replayable JSONL recording. */
export const JSONL_REPLAY_FORMAT = 'regrip';
/** Current schema version for replayable JSONL recordings. */
export const JSONL_REPLAY_VERSION = 1;
